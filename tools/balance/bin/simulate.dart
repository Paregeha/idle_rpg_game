import 'dart:io';

/// Balance simulator CLI — implemented in `T-018`.
///
/// Placeholder so the package has an entrypoint and takes part in the
/// workspace resolution from day one.
void main(List<String> args) {
  stdout.writeln('balance simulator: not implemented yet (see T-018)');
}
