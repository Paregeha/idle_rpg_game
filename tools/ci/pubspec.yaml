name: ci_tools
description: >-
  Checks that run in CI and guard the architectural rules the analyzer cannot
  express. See `T-002` in TASKS.md.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  yaml: ^3.1.3

dev_dependencies:
  test: ^1.25.5
  very_good_analysis: ^10.2.0
